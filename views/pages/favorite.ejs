<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="../styles/style.css">
    <%-include("../layouts/head")%>
    <title>Favorite</title>
</head>

<body>
    <header>
            <%-include("../layouts/header")%>
    </header>

    <main>
        <div class="all">
            <% data.forEach((vals)=>{%>
            <div class="container">
                <h2>Jokes #</h2>
                <h3><%=vals.type%></h3>
                <p><%=vals.setup%></p>
                <address class="author"><%=vals.punchline%></address>
                <a href="/details/<%=vals.id%>"><input type="submit" value="Details"></a>
            </div>
            <%})%>
        </div>
    </main>

    <footer>
    <%-include("../layouts/footer")%>
    </footer>

</body>

</html>